class ReactManager {
  constructor () {
    this.guilds = {}
    this.channels = {}
  }

  manage (msg) {

  }

  check (msg) {
    if (this.channels[msg.channel.id])
  }

  addReactListener()
}

module.exports = new ReactManager
